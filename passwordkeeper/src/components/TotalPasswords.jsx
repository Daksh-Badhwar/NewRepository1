// src/components/TotalPasswords.jsx
import React from 'react';

const TotalPasswords = ({ count }) => {
  return <p style={{ textAlign: 'center' }}>Total Passwords: {count}</p>;
};

export default TotalPasswords;
